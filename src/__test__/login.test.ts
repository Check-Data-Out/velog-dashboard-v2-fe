describe('로그인 화면에서', () => {
  it('액세스 토큰과 리프레시 토큰을 입력하면 버튼이 활성화된다', () => {
    expect(1).toBe(1);
  });

  it('액세스 토큰이 정상적이지 않으면 오류 토스트가 표기된다', () => {
    expect(1).toBe(1);
  });

  it('페이지에 접속했을 때, 세션ID가 있으면 페이지를 이동시킨다', () => {
    expect(1).toBe(1);
  });
});
